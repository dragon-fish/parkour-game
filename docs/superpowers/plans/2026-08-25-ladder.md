# 爬梯子 / 竖直水管（Ladder）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 复刻 ME 的梯子/竖管：正面吸附、碰撞式攀爬、GrabJump 同构跳出、
定向快捷吸附、ClimbUp 式顶端退出；顺带抽 LineMove 基类并给滑索碰撞脱线。

**Architecture:** InterestLine 家族第三个客户。新 `LadderMove` 是状态承载型
Move（同 Zipline/Swing），从新抽的 `LineMove` 基类继承磁吸淡入/转身/冷却/
碰撞位移；顶端退出是 move 内嵌的 ScriptedMove 相位（GrabMove mantle 同构）。

**Tech Stack:** Godot 4.7.1 GDScript，GUT 测试（`tools/run_tests.sh <pattern>`）。

**Spec:** `docs/superpowers/specs/2026-08-25-ladder-design.md`（本 plan 的
唯一权威；冲突以 spec 为准）。

## Global Constraints

- 分支 `feat/ladder`；提交信息纯英文 Conventional Commits。
- 任何 Godot 一律用 `.engine/Godot_v4.7.1-stable_macos.universal.app/Contents/MacOS/Godot`，
  **只许 headless**（测试跑 `tools/run_tests.sh <pattern>`），绝不打开带窗编辑器/游戏。
- 不碰 `me_level*` 与 `_local/`。
- 代码注释英文；作者裁定引用可保留中文原话（现有文件通例）。
- 所有新旋钮进 `LadderConfig`，不许硬编码散落；⚠️ 数值全是待实测初值。
- 表现值不写断言（作者规则）：测试只钉结构不变量（门、优先级、脱线、
  🔒受保护技巧），不断言具体速度/时长数值。
- Task 2 的硬门槛：`tools/run_tests.sh zipline_move swing_move` 在重构后必须
  与重构前**逐条同绿**（22 + 14）。

---

### Task 1: InterestLine 的 LADDER 类别与正面方向

**Files:**
- Modify: `scripts/level/interest_line.gd`
- Test: `tests/test_ladder_move.gd`（新建，先只放这一条）

**Interfaces:**
- Produces: `InterestLine.Kind.LADDER`；`func front() -> Vector3`（节点 -Z 的
  水平归一化，玩家站的那一侧）；InterestLine 进组 `"interest_lines"`（Task 6
  的远距扫描要用，volume 进入制之外的唯一发现渠道）。

- [ ] **Step 1: 失败测试**

```gdscript
extends ParkourTest

const TestWorld = preload("res://tests/world_fixture.gd")

func _vertical_ladder(at: Vector3, yaw_deg: float = 0.0) -> InterestLine:
	var line := InterestLine.new()
	line.kind = InterestLine.Kind.LADDER
	line.curve = Curve3D.new()
	line.curve.add_point(Vector3.ZERO)
	line.curve.add_point(Vector3(0.0, 3.0, 0.0))
	line.position = at
	line.rotation.y = deg_to_rad(yaw_deg)
	add_child_autofree(line)
	return line

func test_front_is_the_nodes_minus_z_flattened() -> void:
	var line := _vertical_ladder(Vector3.ZERO, 90.0)
	await step(1)
	# yaw +90: -Z rotates onto -X.
	assert_almost_eq(line.front().x, -1.0, 0.01, "front did not follow the node yaw")
	assert_almost_eq(line.front().y, 0.0, 0.001, "front must be horizontal")
	assert_true(line.is_in_group("interest_lines"), "lines must be discoverable for the snap scan")
```

- [ ] **Step 2: 跑测确认失败**（`tools/run_tests.sh ladder_move`，期待
  `front` 不存在 / 不在组）
- [ ] **Step 3: 实现**——enum 加 `LADDER`；`_ready()` 里 `add_to_group("interest_lines")`；

```gdscript
## The side a body climbs this line from: the node's own -Z, flattened.
## Meaningful for LADDER (the authored "正面"); other kinds never ask.
func front() -> Vector3:
	var f: Vector3 = -global_transform.basis.z
	f.y = 0.0
	return f.normalized() if f.length_squared() > 0.0001 else Vector3.FORWARD
```

- [ ] **Step 4: 跑测转绿**
- [ ] **Step 5: Commit** `feat(level): interest lines learn LADDER and their own front`

### Task 2: LineMove 基类抽取（行为零变化）

**Files:**
- Create: `scripts/player/moves/line_move.gd`
- Modify: `scripts/player/moves/zipline_move.gd`, `scripts/player/moves/swing_move.gd`
- Test: 既有 `tests/test_zipline_move.gd`、`tests/test_swing_move.gd`（不改内容）

**Interfaces:**
- Produces（子类可用）: `var _line: InterestLine`、`var _aborted: bool`、
  `var _fade: float`、`var _entry_pos: Vector3`、`var _entry_yaw: float`、
  `var _target_yaw: float`、`var _fan_centred: bool`；
  `func acquire_line(kind: InterestLine.Kind) -> bool`（找线、失败置 _aborted）；
  `func _turn_body_to(yaw: float) -> void`；`func _centre_fan() -> void`；
  `func note_left(redo_time: float) -> void`（exit 用，含 is_instance_valid 守卫）；
  `func slide_to(target: Vector3) -> KinematicCollision3D`（见 Step 3）。

- [ ] **Step 1: 先记基线**——`tools/run_tests.sh zipline_move swing_move`，
  记录 22/22 与 14/14 全绿（若不同，停下报告，不得继续）。
- [ ] **Step 2: 建基类**——把 Zipline/Swing 里**逐字重复**的成员与
  `_turn_body_to`/`_centre_fan` 上移（两个文件里的实现完全同构，以
  zipline_move.gd 的版本为准）；加共享位移助手：

```gdscript
class_name LineMove
extends Move

# Shared skeleton of the "along a line" family (zipline / swing / ladder):
# line acquisition, magnet fade bookkeeping, the turn-across-the-fade pair,
# per-line cooldown on the way out, and collision-checked placement for the
# members that ride NEAR level geometry. Subclasses own their physics.

## Moves the body toward `target` THROUGH the physics world instead of
## teleporting it there. Returns the collision if the way was blocked --
## ✅ the owner: designers deliberately sink line ends into walls, and a
## rider must bounce off geometry rather than pass into it.
func slide_to(target: Vector3) -> KinematicCollision3D:
	return player.move_and_collide(target - player.global_position)
```

- [ ] **Step 3: 两个子类改继承**，删除上移的成员；swing 的
  `_window_grace`/`_theta` 等私有物理留在子类。Zipline 的 exit 冷却与
  swing 的统一改调 `note_left(cfg.same_line_redo_time)`。
- [ ] **Step 4: 跑基线测试**——两套件逐条同绿；再跑
  `tools/run_tests.sh clip_preemption death_to_respawn` 防误伤。
- [ ] **Step 5: Commit** `refactor(moves): the line family shares one spine`

### Task 3: 滑索碰撞脱线

**Files:**
- Modify: `scripts/player/moves/zipline_move.gd`（每 tick 落位改走 `slide_to`）
- Test: `tests/test_zipline_move.gd` 追加一条

**Interfaces:**
- Consumes: Task 2 的 `slide_to`。

- [ ] **Step 1: 失败测试**（在既有 fixture 风格里：拉一根穿进墙的缆绳，
  骑到撞墙）

```gdscript
func test_riding_into_a_wall_forces_the_release() -> void:
	# ✅ The owner, on the original: "滑到底忘记放手撞到墙被弹出去了."
	# A cable whose far end sits INSIDE a wall must throw the rider off at
	# the wall, never carry the capsule through it.
	# fixture: wall (StaticBody3D+BoxShape3D) planted across the cable's
	# path at x=4; cable from (0,3,0) to (8,1,0); player attached at start.
	# ...(按本套件既有 fixture 写法搭建，位置先于 add_child -- 见
	# tests/test_checkpoints.gd 的 fixture 注释教训)...
	# ride until either exit or 5 s timeout:
	# assert: move left ZIPLINE before reaching x=4.5, player.global_position.x < 4.5
```

（实测断言写成"离开 ZIPLINE 状态且未越过墙面"，不断言反弹速度数值。）

- [ ] **Step 2: 跑测确认失败**（当前实现直接写 global_position，会穿墙）
- [ ] **Step 3: 实现**——`physics_update` 里 `player.global_position = pos`
  改为 `var hit := slide_to(pos)`；`hit != null` 时：残余速度 =
  当前缆绳速度在碰撞面上的滑动分量（`v.slide(hit.get_normal())`），
  写进 `player.velocity`，`return FALLING`。
- [ ] **Step 4: 全套件回归** `tools/run_tests.sh zipline_move`
- [ ] **Step 5: Commit** `feat(zipline): geometry throws the rider off`

### Task 4: LadderMove 核心（配置、注册、正面门、攀爬、两端、松手）

**Files:**
- Create: `scripts/player/config/moves/ladder_config.gd`,
  `scripts/player/moves/ladder_move.gd`
- Modify: `scripts/player/config/movement_config.gd`（`@export var ladder`），
  `scripts/player/moves/move.gd`（`const LADDER: StringName = &"Ladder"`），
  `scripts/player/player.gd:1107` 注册表加一行，
  `scripts/player/moves/airborne_move.gd`（probe_transition 加 LADDER 块，
  紧跟 SWING 块之后、GRAB 之前），
  `scripts/player/config/moves/airborne_config.gd`（`check_for_ladder` 旗标，
  falling/jump 两个子配置置 true），
  `scripts/player/moves/walking_move.gd`（地面走进正面体积也吸附），
  `scripts/player/moves/wall_run_move.gd`（滑墙准入，✅ 作者：ME 有一关
  就是滑墙接水管）
- Test: `tests/test_ladder_move.gd`

**Interfaces:**
- Produces: `LadderMove`（`_line`、`_offset: float` 当前弧长、
  `func climbing_offset() -> float` 供测试/HUD）；
  `LadderConfig` 旋钮：`climb_speed=1.8`、`fade_in_time=0.15`、
  `stand_off=0.4`（胶囊中心离线的正面距离——spec 未列，实现必需：胶囊
  不能嵌进梯子所在的墙）、`jump_angle_deg=45.0`、`jump_speed`（初值抄
  GrabConfig 同名跳速）、`top_exit_reach=1.2`、`top_exit_time=2.0`、
  `snap_range=4.0`、`snap_flight_time=0.55`、`same_line_redo_time=0.6`、
  `fall_limit=6.0`。
- Consumes: Task 1 `front()`；Task 2 基类全部。

- [ ] **Step 1: 失败测试**（核心六条）

```gdscript
# fixture: TestWorld + _vertical_ladder(...)（Task 1 的），墙贴在梯子 +Z 侧。
func test_a_front_approach_attaches_and_a_back_one_does_not() -> void:
	# front half-space player walks in -> current move becomes LADDER;
	# back-side player passing the same volume stays FALLING/GROUNDED.
func test_climbing_moves_along_the_line() -> void:
	# hold W N ticks -> global_position.y strictly rises; S -> falls.
func test_the_floor_ends_a_descent() -> void:
	# line authored to run PAST the floor (✅ the owner: designers sink line
	# ends into geometry); climb down -> move exits, capsule never below floor.
func test_a_ceiling_stops_the_ascent() -> void:
	# slab above; climb up -> y stops rising, move does NOT exit.
func test_crouch_lets_go_and_spends_the_roll() -> void:
	# crouch_pressed -> FALLING and player's roll buffer consumed
	# (mirror test_swing_move's existing consume_roll assertion shape).
func test_wallrun_can_be_caught_by_a_ladder() -> void:
	# wallrun state entering the front volume -> LADDER (✅ the owner).
```

（每条按 test_swing_move.gd 的既有 fixture/step 习惯写全；此处列意图与
断言对象，实现者补搭建代码。）

- [ ] **Step 2: 跑测确认失败**
- [ ] **Step 3: 实现** `ladder_config.gd`（MoveConfig 子类照抄 SwingConfig
  文件形状）＋ `ladder_move.gd` 核心：

```gdscript
class_name LadderMove
extends LineMove

# The original's TdMove_Ladder. ✅ The owner: pipes and ladders are one
# mechanic with an authored FRONT ("梯子只有一面可以进入"); entry is a
# frontal 180-degree fan (front half-space), open to airborne, grounded
# AND wallrunning bodies.

var _offset: float = 0.0

func enter(_previous: StringName) -> void:
	player.set_grounded(false)
	_aborted = not acquire_line(InterestLine.Kind.LADDER)
	if _aborted:
		return
	if not LadderMove.front_side_allows(_line, player.global_position):
		_aborted = true
		return
	_offset = _line.closest_offset(player.global_position)
	# fade bookkeeping + target yaw facing the ladder (looking along -front):
	var f: Vector3 = _line.front()
	_target_yaw = atan2(f.x, f.z)   # yaw that LOOKS toward -front
	# ... _entry_pos/_entry_yaw/_fade = 0 as the family does ...
	player.velocity = Vector3.ZERO

## The frontal gate, static so AirborneMove/WalkingMove/WallRunMove ask the
## SAME question before ever transitioning (no enter-then-abort flutter).
static func front_side_allows(line: InterestLine, body_pos: Vector3) -> bool:
	var at: Vector3 = line.sample(line.closest_offset(body_pos))["position"]
	var to_body: Vector3 = body_pos - at
	to_body.y = 0.0
	return to_body.dot(line.front()) > 0.0

func physics_update(delta: float, input: MoveInput) -> StringName:
	if _aborted or not is_instance_valid(_line):
		return FALLING
	player.set_grounded(false)
	player.fall_tracker.reset(player.global_position.y)
	if input.crouch_pressed:
		player.consume_roll()
		return FALLING
	# ... jump chain lands here in Task 5, top exit in Task 7 ...
	_offset = clampf(_offset + input.move.y * cfg.climb_speed * delta, 0.0, _line.length())
	var s: Dictionary = _line.sample(_offset)
	var hang: Vector3 = s["position"] + _line.front() * cfg.stand_off
	# fade-in: lerp _entry_pos -> hang + _turn_body_to(lerp_angle(...)),
	# then _centre_fan() once -- byte-for-byte the family shape.
	var hit: KinematicCollision3D = slide_to(hang)
	if hit != null:
		# Feet on the floor while descending IS the bottom (✅ the owner):
		if input.move.y < 0.0 and hit.get_normal().y > 0.5:
			return FALLING            # next tick lands and grounds normally
		# A ceiling (or anything else): stay, but do not fight it.
		_offset = _line.closest_offset(player.global_position - _line.front() * cfg.stand_off)
	return KEEP

func exit() -> void:
	note_left(cfg.same_line_redo_time)
```

  注册与捕获：`move.gd` 加 const；`player.gd` 表加行；`airborne_move.gd`
  在 SWING 块后加同构块（`check_for_ladder` + `fall_limit` 门 +
  `front_side_allows` 门）；`walking_move.gd`/`wall_run_move.gd` 各加
  一个"体积内且正面且 can_enter"检查。F1 面板：确认 TuningPanel 从
  MovementConfig 导出表自动拾取 `ladder` 组（现有机制，无代码则说明）。
- [ ] **Step 4: 跑测转绿** `tools/run_tests.sh ladder_move`；回归
  `zipline_move swing_move walking`（如有）
- [ ] **Step 5: Commit** `feat(moves): the ladder -- frontal catch, collision-checked climb`

### Task 5: 跳出链（视线跳；快捷吸附占位为 null）

**Files:**
- Modify: `scripts/player/moves/ladder_move.gd`
- Test: `tests/test_ladder_move.gd` 追加

**Interfaces:**
- Produces: `func _scan_snap_target(side: int) -> InterestLine`（本任务恒
  返回 null，Task 6 填实；side: -1 左 / +1 右 / 0 背后）；跳出优先级链
  即 spec 的 1→2→3。

- [ ] **Step 1: 失败测试**

```gdscript
func test_space_facing_the_ladder_does_nothing() -> void:
	# camera square to the ladder, jump_pressed -> still LADDER.
func test_a_turned_head_jumps_along_the_look() -> void:
	# camera yawed 60 deg off, jump_pressed -> FALLING, velocity dot
	# look_direction > 0, vertical component follows the pitch.
func test_the_into_wall_component_survives() -> void:
	# 🔒 protected tech, invariant 1 (spec): camera 46 deg off, jump ->
	# velocity keeps a positive component INTO the ladder's wall side
	# (dot with -front() > 0). Killing this deletes the vault finisher.
```

- [ ] **Step 2: 确认失败**
- [ ] **Step 3: 实现**——`physics_update` 蹲判定之后：

```gdscript
	if input.jump_pressed:
		var side: int = 0
		if absf(input.move.x) > 0.1:
			side = 1 if input.move.x > 0.0 else -1
		var target: InterestLine = _scan_snap_target(side)
		if target != null:
			return _launch_at(target)        # Task 6
		var turned: float = absf(wrapf(_camera_yaw() - _target_yaw, -PI, PI))
		if turned > deg_to_rad(cfg.jump_angle_deg):
			# GrabMove's shape verbatim: the full 3D look, nothing projected
			# out -- the into-wall component IS the vault tech (grab_move.gd
			# _launch_direction's own warning applies here unchanged).
			player.velocity = _look_direction() * cfg.jump_speed
			player.consume_roll()
			return FALLING
		# Facing the ladder, no target: the press is IGNORED (✅ the owner:
		# "AD+空格如果没有其他梯子是不会触发跳的").
```

  `_look_direction()`/`_camera_yaw()` 各 ~5 行，照 grab_move.gd 646 起的
  写法读 `player.camera_rig.camera`，空守卫回退到身体朝向。
- [ ] **Step 4: 转绿 + 回归 ladder 全套件**
- [ ] **Step 5: Commit** `feat(ladder): the jump chain -- look-jumps with the glitch intact`

### Task 6: 快捷吸附

**Files:**
- Modify: `scripts/player/moves/ladder_move.gd`
- Test: `tests/test_ladder_move.gd` 追加

**Interfaces:**
- Consumes: Task 1 的 `"interest_lines"` 组与 `front()`；`player.line_ready()`。
- Produces: `_scan_snap_target` 实做；`func _launch_at(line) -> StringName`。

- [ ] **Step 1: 失败测试**

```gdscript
func test_a_side_hop_snaps_to_the_neighbour_ladder() -> void:
	# second ladder 2.5 m to this one's left; A+space -> within 2 s the
	# current move is LADDER again and _line is the NEIGHBOUR (assert via
	# player position near it; no camera aiming was set up at all).
func test_no_neighbour_means_no_hop() -> void:
	# nothing within snap_range on the left; A+space -> still LADDER,
	# position unchanged (✅ the owner).
func test_the_scan_respects_direction() -> void:
	# neighbour on the LEFT but D+space (right) pressed -> no hop.
```

- [ ] **Step 2: 确认失败**
- [ ] **Step 3: 实现**

```gdscript
func _scan_snap_target(side: int) -> InterestLine:
	# Direction in the LADDER's frame (✅ no camera aiming): left/right run
	# along front x up; 0 means straight back off the ladder.
	var f: Vector3 = _line.front()
	var dir: Vector3 = f if side == 0 else Vector3.UP.cross(f) * -float(side)
	var best: InterestLine = null
	var best_d: float = cfg.snap_range
	for node in player.get_tree().get_nodes_in_group("interest_lines"):
		var line := node as InterestLine
		if line == null or line == _line or not player.line_ready(line):
			continue
		var at: Vector3 = line.sample(line.closest_offset(player.global_position))["position"]
		var to: Vector3 = at - player.global_position
		var d: float = to.length()
		if d > best_d or d < 0.001:
			continue
		if to.normalized().dot(dir) < 0.7:   # ~45-degree cone in the pressed direction
			continue
		best_d = d
		best = line
	return best

func _launch_at(line: InterestLine) -> StringName:
	# Ballistic through the target's own catch volume: given flight time,
	# solve v0 so the arc passes the line's nearest point; the target's own
	# catch logic does the rest -- this move places NOTHING by hand.
	var at: Vector3 = line.sample(line.closest_offset(player.global_position))["position"]
	var t: float = cfg.snap_flight_time
	var g: float = player.effective_gravity()
	player.velocity = (at - player.global_position) / t + Vector3.UP * (0.5 * g * t)
	player.consume_roll()
	return FALLING
```

  ⚠️ `0.7` 锥门是唯一内联常数，实现时提为 `LadderConfig.snap_cone_dot`
  （初值 0.7）——规矩：旋钮进配置。
- [ ] **Step 4: 转绿 + 回归**
- [ ] **Step 5: Commit** `feat(ladder): assisted hops -- Faith jumps for you`

### Task 7: 顶端退出（探测 + ClimbUp 式脚本搬运）

**Files:**
- Modify: `scripts/player/moves/ladder_move.gd`
- Test: `tests/test_ladder_move.gd` 追加

**Interfaces:**
- Consumes: `ScriptedMove.begin/advance/sample`（scripts/player/moves/
  scripted_move.gd——GrabMove 的 mantle 相位是唯一现成用例，照它接：
  含 scripted 片段 gate 的申请、视点跟随权重、结束转 GROUNDED）。

- [ ] **Step 1: 失败测试**

```gdscript
func test_top_plus_w_carries_the_body_onto_the_deck() -> void:
	# platform behind the ladder top within top_exit_reach; climb to top,
	# keep holding W -> after the scripted phase the body stands ON the
	# platform (grounded, y ~ platform top), never teleported (sample mid-
	# phase: position strictly between start and end).
func test_no_deck_means_no_exit() -> void:
	# nothing standable behind the top -> W at the top keeps KEEP; y capped.
func test_space_beats_the_top_exit() -> void:
	# 🔒 protected tech, invariant 3: at the very top, camera turned 50 deg,
	# jump_pressed while W held -> the JUMP fires (FALLING), not the
	# scripted exit.
```

- [ ] **Step 2: 确认失败**
- [ ] **Step 3: 实现**——顶端判定（`_offset >= _line.length() - 0.01` 且
  `input.move.y > 0`）时探测：从「顶点 + (-front()) * reach」上方 0.5 m
  向下 ray（`player.get_world_3d().direct_space_state`，照 probes.gd 的
  查询写法），命中面 `normal.y > 0.7` 且 `player.fits_standing_at(hit)`
  才算可站；然后进入内嵌脚本相位：`ScriptedMove.begin(当前位置, 落点,
  cfg.top_exit_time)`，每 tick `advance+sample` 写位置（此相位免碰撞——
  搬运曲线本来就穿沿口），动画申请 `&"ClimbUp_1m"` 走 scripted gate
  （申请 API 与权重形状照 GrabMove mantle 相位抄，✅ 作者怀疑 ME 就是
  ClimbUp 逻辑）；结束 `return GROUNDED`。跳出链判定必须在顶端判定
  **之前**（Task 5 已保证顺序：蹲→跳→顶端→攀爬）。
- [ ] **Step 4: 转绿 + 全家族回归**
  `tools/run_tests.sh ladder_move zipline_move swing_move clip_preemption`
- [ ] **Step 5: Commit** `feat(ladder): the top exit is a ClimbUp, and space still wins`

### Task 8: 动画 arm、文档、收尾

**Files:**
- Modify: `scripts/player/character_animator.gd`（`_route` 加 `Move.LADDER`
  arm，紧跟 SWING：`return _first_available([&"Climb_Idle", &"NinjaJump_Idle",
  &"Jump", &"jump", &"idle"])`，注释注明是占位、真攀爬循环是已知缺口），
  `docs/level-templates.md`（"Placing a ladder"节：竖直画线、-Z 朝外、
  kind=LADDER、末端可埋墙），
  `docs/superpowers/specs/2026-08-25-ladder-design.md`（如实现中有偏离，
  更新为 as-built）
- Test: `tools/run_tests.sh` 全量

- [ ] **Step 1: animator arm + 文档**
- [ ] **Step 2: 全量回归**——基线是 master 的 7 个已知失败（4 个存量
  文件），**不得多一个**；多了就修到回基线（参考 test_death_sequence
  的 teardown 教训：建了 world 必须 TestWorld.teardown）。
- [ ] **Step 3: Commit** `feat(ladder): animator arm and level docs`

## Self-Review 记录

- Spec 覆盖：标记（T1）、正面门+滑墙准入（T4）、碰撞攀爬/两端（T4）、
  跳出链+🔒不变量1（T5）、吸附（T6）、🔒不变量2·3+顶端（T5/T7 测试）、
  滑索脱线（T3）、LineMove（T2）、旋钮（T4）、文档（T8）。无缺口。
- 待实测数值全部为旋钮初值，已在 Global Constraints 声明不写数值断言。
- 类型一致性：`front()`/`front_side_allows`/`_scan_snap_target`/`_launch_at`
  各任务间签名一致。

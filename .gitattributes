# Normalize EOL for all files that Git considers text files.
* text=auto eol=lf

# ---------------------------------------------------------------------------
# Git LFS
#
# Binary assets are stored in LFS so that repeated edits do not bloat history.
# The `-text` flag is required: it opts these paths out of the `* text=auto`
# rule above, which would otherwise attempt EOL normalization on binary data.
#
# Only genuinely binary formats belong here. Godot's text-based counterparts
# (.tscn, .tres, .gd, .import, .svg, .gltf, .obj, .dae) must stay in plain Git
# so they remain diffable and mergeable.
# ---------------------------------------------------------------------------

# Images
*.png   filter=lfs diff=lfs merge=lfs -text
*.jpg   filter=lfs diff=lfs merge=lfs -text
*.jpeg  filter=lfs diff=lfs merge=lfs -text
*.webp  filter=lfs diff=lfs merge=lfs -text
*.tga   filter=lfs diff=lfs merge=lfs -text
*.bmp   filter=lfs diff=lfs merge=lfs -text
*.psd   filter=lfs diff=lfs merge=lfs -text
*.dds   filter=lfs diff=lfs merge=lfs -text
*.ktx   filter=lfs diff=lfs merge=lfs -text

# HDR / environment maps
*.hdr   filter=lfs diff=lfs merge=lfs -text
*.exr   filter=lfs diff=lfs merge=lfs -text

# Audio
*.wav   filter=lfs diff=lfs merge=lfs -text
*.ogg   filter=lfs diff=lfs merge=lfs -text
*.mp3   filter=lfs diff=lfs merge=lfs -text
*.flac  filter=lfs diff=lfs merge=lfs -text

# 3D models (binary formats only)
*.glb   filter=lfs diff=lfs merge=lfs -text
*.fbx   filter=lfs diff=lfs merge=lfs -text
*.blend filter=lfs diff=lfs merge=lfs -text
*.bin   filter=lfs diff=lfs merge=lfs -text

# Fonts
*.ttf   filter=lfs diff=lfs merge=lfs -text
*.otf   filter=lfs diff=lfs merge=lfs -text
*.woff2 filter=lfs diff=lfs merge=lfs -text

# Video
*.ogv   filter=lfs diff=lfs merge=lfs -text
*.webm  filter=lfs diff=lfs merge=lfs -text
*.mp4   filter=lfs diff=lfs merge=lfs -text

# Godot binary resources (.tres / .tscn are text and stay out of LFS)
*.res   filter=lfs diff=lfs merge=lfs -text
*.scn   filter=lfs diff=lfs merge=lfs -text

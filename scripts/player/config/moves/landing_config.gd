class_name LandingConfig
extends MoveConfig

# The Godot counterpart of the original's TdMove_Landing. No fields of its
# own yet -- landing thresholds still read straight off PawnConfig's Landing
# group -- so this class exists purely to give Landing a slot in
# MovementConfig that matches every other move's own layering.
